-- require("mason").setup({})
-- require("mason-lspconfig").setup {
  -- ensure_installed = { "ts_ls", "lua_ls" }, -- Install desired servers
-- }

-- Configure tsserver
-- local lspconfig = require("lspconfig")

-- lspconfig.tsserver.setup({
  -- on_attach = function(client, bufnr)
    -- print("TypeScript LSP attached.")
  -- end,
-- })